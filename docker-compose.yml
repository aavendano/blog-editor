services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: blog_editor
      POSTGRES_PASSWORD: blog_editor
      POSTGRES_DB: blog_editor
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
